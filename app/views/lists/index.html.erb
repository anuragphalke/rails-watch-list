<h1>Lists</h1>

<% @lists.each do |list| %>
  <div>
    <h3><%= link_to list.name, list_path(list) %></h3>
    <p><%= list.name %></p>
  </div>
<% end %>

<%= link_to 'Create New List', new_list_path %>
